---
// The site footer now renders full-width at the bottom of the page from
// CustomPageFrame.astro (so it spans the whole viewport rather than the content
// column). This override stays registered to suppress Starlight's default
// in-content footer (pagination / edit link / last updated). It intentionally
// renders nothing.
---
